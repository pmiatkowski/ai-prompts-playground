# Testing Guidelines

- If you have a file `component.tsx`, the test file should be named `component.test.tsx` and placed in the same directory.
- If you have a file `utils.js`, the test file should be named `utils.test.js` and placed in the same directory.
