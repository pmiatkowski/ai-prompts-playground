# React Guidelines

- Use named functions instead of arrow functions when creating react components.
- Use `Props` as a type for component props.
- For React components, never `import React from 'react'`; React is available globally.
